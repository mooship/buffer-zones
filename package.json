{
  "name": "stratum",
  "version": "1.0.0",
  "description": "Reusable SDK for public-interest geospatial layer platforms",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "npm run test:e2e --workspace @stratum/web",
    "typecheck": "npm run typecheck --workspace @stratum/core && npm run typecheck --workspace @stratum/app && npm run typecheck --workspace @stratum/map && npm run typecheck --workspace @stratum/react && npm run build --workspace @stratum/web && npm run typecheck --prefix data-pipeline",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@vitest/coverage-v8": "^4.1.10",
    "lefthook": "^2.1.10",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  }
}
