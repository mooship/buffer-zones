{
  "name": "buffer-zones",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "vitest run",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "lefthook": "^1.7.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6",
    "wrangler": "^4.114.0"
  }
}
